把全部的api文档对接上，指的是在前端的api目录里面创建对应的api文件
并且能够调通接口，然后把字段的注释加上

media/枫枫商城.openapi.json
这个是后端的api接口文档

后端已经运行起来了在 127.0.0.1:8080
后端项目地址在 F:\IT\fengfeng\fengfeng_mall\fm_server
如果接口报错，可以通过日志排查问题

对接完之后把代码提交到远程仓库，提交信息需要中文
